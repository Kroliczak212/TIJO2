// Setup file for Jest
// You can add global mocks or configuration here

// Increase timeout for integration tests
jest.setTimeout(30000);

// Mock console.log/error to keep test output clean, if desired
// console.log = jest.fn();
// console.error = jest.fn();
